See the sample scripts for an example.

- sample_convert_all_characters.rb creates a new .html file for .dnd4e file.
- sample_create_summary.rb creates a single page with just the summary for each character

Requires andand, nokogiri, mechanize.
